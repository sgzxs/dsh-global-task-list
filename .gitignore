# Dependencies
node_modules/

# Build artifacts (rebuilt by `prepare` on install / `npm run build:client`)
lib/
*.tgz

# Lockfile (dev-only; consumers resolve via peerDependencies against DSH)
package-lock.json
